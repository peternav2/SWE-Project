export default function DayButton() {
    return (
        <div>
            {/*someone make this link to a route which displays the menu for that day.*/}
            <h1> DayButton </h1>
        </div>
    )
}