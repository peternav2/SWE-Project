export default function Loading() {
  return (
    <div className="flex justify-center items-center h-screen">
      {/* create a loading page, maybe even have an optional prop which will be used for display*/}
      {/*eg "Loading { string prop }*/}
      {/*eg "Loading University */}
      <h1> Loading </h1>
    </div>
  )
}